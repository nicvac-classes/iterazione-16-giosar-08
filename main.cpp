using namespace std;

int main() {
  int N(0), i(0);
  float num(0), nmax(0);

  cout << "Quanti numeri inserire?" << endl;
  cin >> N;
  
  for (i = 1; i <= N<i; i) {
    cout << "Inserisci il " << i << " numero" << endl;
    cin >> i;
    if (i == 1) {
      max = i;
    }
    if (i > nmax) {
      max = x;
    }
  }
  
  cout << "Il massimo è " << nmax << endl;
}
